const step10 = `
We're getting closer to dispatching. In this step, DataLoader either breaks dispatch into chunks if you set a maxBatchSize, 
or dispatches all the keys to be loaded in one go otherwise. In the next step we'll send off our keys and receieve values! 
`;

export default step10;
